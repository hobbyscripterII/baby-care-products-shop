<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.baby.babycareproductsshop.order.OrderDetailMapper">
    <insert id="insOrderDetail">
        <selectKey keyProperty="idetails" resultType="int" order="BEFORE">
            SELECT MAX(idetails) + 1
            FROM t_order_details
        </selectKey>
        INSERT INTO t_order_details
        SET iorder = #{iorder},
            iproduct = #{iproduct},
            idetails = #{idetails},
            product_cnt = #{productCnt},
            product_price = #{productPrice},
            product_total_price = #{productTotalPrice}
    </insert>

    <select id="selOrderDetailsForPurchase">
        SELECT B.product_nm, A.Product_cnt, A.product_total_price, B.rep_pic
        FROM t_order_details A
        JOIN t_product B
        USING (iproduct)
        WHERE iorder = #{iorder}
    </select>
</mapper>